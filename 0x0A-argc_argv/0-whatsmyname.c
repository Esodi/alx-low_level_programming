#include <stdio.h>

/**
 * main - big function
 * @argc: argument count
 * @argv[]: pointer to arg vector
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	argc = 0;
	printf("%s\n", argv[argc]);

	return (0);
}
